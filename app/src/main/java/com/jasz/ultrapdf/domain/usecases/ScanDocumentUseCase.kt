package com.jasz.ultrapdf.domain.usecases

import javax.inject.Inject

class ScanDocumentUseCase @Inject constructor() {
    // This use case is no longer needed with the ML Kit Document Scanner API
    // The logic will be handled in the ViewModel and View
}
